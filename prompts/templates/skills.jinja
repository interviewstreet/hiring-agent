Extract ONLY the skills information from this resume.

{% if context_snippet %}
--- RESUME CONTEXT ---
{{ context_snippet }}
--- END CONTEXT ---

{% endif %}
--- TARGET SECTION: Skills ---

{{ target_section_text }}

--- END TARGET SECTION ---

{% if context_snippet %}
**INSTRUCTIONS**: Using the CONTEXT for background understanding, extract ONLY the skills from the TARGET SECTION text into the required JSON format.
{% endif %}

Return ONLY a JSON object with this structure:
{
  "skills": [
    {
      "name": "Skill category",
      "level": null,
      "keywords": ["Skill 1", "Skill 2"]
    }
  ]
}

**IMPORTANT**: Return ONLY valid JSON. Do not include any explanatory text. 