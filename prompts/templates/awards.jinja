Extract ONLY the awards and honors information from this resume.

{% if context_snippet %}
--- RESUME CONTEXT ---
{{ context_snippet }}
--- END CONTEXT ---

{% endif %}
--- TARGET SECTION: Awards ---

{{ target_section_text }}

--- END TARGET SECTION ---

{% if context_snippet %}
**INSTRUCTIONS**: Using the CONTEXT for background understanding, extract ONLY the awards from the TARGET SECTION text into the required JSON format.
{% endif %}

Return ONLY a JSON object with this structure:
{
  "awards": [
    {
      "title": "Award name",
      "date": "Award date (YYYY-MM)",
      "awarder": "Awarding organization"
    }
  ]
}

**IMPORTANT**: Return ONLY valid JSON. Do not include any explanatory text. 