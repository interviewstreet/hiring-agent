Extract ONLY the projects information from this resume.

{% if context_snippet %}
--- RESUME CONTEXT ---
{{ context_snippet }}
--- END CONTEXT ---

{% endif %}
--- TARGET SECTION: Projects ---

{{ target_section_text }}

--- END TARGET SECTION ---

{% if context_snippet %}
**INSTRUCTIONS**: Using the CONTEXT for background understanding, extract ONLY the projects from the TARGET SECTION text into the required JSON format.
{% endif %}

Return ONLY a JSON object with this structure:
{
  "projects": [
    {
      "name": "Project name",
      "description": "Project description",
      "url": "Project URL",
      "technologies": ["Tech 1", "Tech 2"]
    }
  ]
}

**IMPORTANT**: Return ONLY valid JSON. Do not include any explanatory text. 